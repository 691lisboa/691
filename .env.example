# 691 Lisboa — example configuration (never commit real secrets)
PORT=5000
NODE_ENV=production
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TOMTOM_API_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:jose@79.pt
TELEGRAM_WEBHOOK_URL=https://691.pt/telegram/webhook
TELEGRAM_WEBHOOK_SECRET=
# Required: independent secret used only to protect private booking links/sessions.
# Generate, for example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BOOKING_ACCESS_SECRET=
# Required in production for durable persistence
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

